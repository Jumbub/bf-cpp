#include <iostream>
#include "../brainfuck/file.h"
#include "../brainfuck/parse.h"
#include "ApprovalTests.hpp"
#include "gtest/gtest.h"

TEST(parse, empty) {
  /* const auto code = brainfuck::read("src/tests/programs/empty.b"); */
  /* ASSERT_TRUE(code.has_value()); */
  /* const auto instructions = brainfuck::parse(code.value()); */
  /* ApprovalTests::Approvals::verify(std::string{instructions.begin(), instructions.end()}); */
}
